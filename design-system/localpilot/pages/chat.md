# Chat — Cursor-type overrides

- Transcript layout, not message cards
- User lines: slight right indent + muted “You”
- Assistant: full width, “LocalPilot” label
- Streaming: blue caret / pulse on label
- Composer: raised surface, 8px radius, border-strong, model + permission inside footer of composer
- Primary send = blue circle/icon button; STOP = red while streaming
- Empty state with logo + short pitch + example chips
- No green send buttons
